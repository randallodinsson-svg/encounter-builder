// ------------------------------------------------------------
// Tactical Overlays — Restored Export
// ------------------------------------------------------------

// This function is required by apexsim-renderer.js.
// You can expand it later with real overlay drawing logic.

export function apexcoreDrawOverlays(ctx, simState) {
  // Safe no-op overlay (does nothing but prevents crashes)
  // Add real overlay drawing here when ready.
}
